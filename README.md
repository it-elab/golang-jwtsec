# golang-jwtsec
JWT Manager with Access and Refresh support, IP and User-Agent verification.

##This module allows:

- Generate Access and Refresh tokens with different TTLs and Cryptographic algorithms
- Check token validity, including IP and user-agent
- Automatically check the outflow of token
- Get a new Access Token by Valid Refresh-Token
